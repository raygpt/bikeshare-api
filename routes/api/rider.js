var express = require('express');
var router = express.Router();

/* GET riders in specific age groups ending their trips at one or more stations on a given day. */
router.post('/', function (req, res, next) {});

module.exports = router;
